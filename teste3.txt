declare
 cont: int;
 media, altura, soma: int;
qtd: string;
 start
 cont = 5;
 soma = 0;
 write("Altura: ");
 read (altura);
 soma = soma + altura * qtd;
 cont = cont - 1;

 write("Media: ");
 write (soma / qtd);
 end
