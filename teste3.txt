declare
 cont: int;
 media, altura, soma: int;
 start
 cont = 5;
 soma = 0;
 do

 write("Altura: ");
 read (altura);
 soma = soma + altura;
 cont = cont - 1;

 while(cont)
 write("Media: ");
 write (soma / qtd);
 end
